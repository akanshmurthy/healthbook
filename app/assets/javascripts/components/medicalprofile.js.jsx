/* global React */

(function(root) {
  'use strict';
  root.MedicalProfile = React.createClass({
    render: function () {
      return(
        <div>
          Welcome to your profile!
        </div>
      );
    }
  });
}(this));
